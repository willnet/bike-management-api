require 'rails_helper'

RSpec.describe BrandsController, type: :controller do
  context 'リクエスト成功時' do
    it 'jsonリクエストを返すこと' do
      # FactoryBot.create(:brand_name_is_giant)
      # FactoryBot.create(:bike_made_by_giant)
      # get :show, format: :json, params: { name: 'giant_brand_bike' }
      # p 'ssssssssssssssssssssssssssssssss'
      # p response
      # expect(response.content_type).to eq 'application/json'
    end
  end
end
